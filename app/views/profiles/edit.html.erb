
<!DOCTYPE html>
<html>
  <body>
    <main class="bg-home">
      <% content_for(:title, 'プロフィール編集') %>
      <div class="container pt-3">
        <div class="row">
          <div class="col-md-10 offset-md-1 mb-5">
            <h1 class="float-left"><%= t('.プロフィール編集') %></h1>
          </div>
          <div class="col-md-10 offset-md-1">
            <%= form_with model: @user, url: profile_path, local: true do |f| %>
              <div class="form-group">
                <%= f.label :Eメール %>
                <%= f.email_field :email, class: "form-control" %>
              </div>
              <div class="form-group">
                <%= f.label :ニックネーム %>
                <%= f.text_field :nickname, class: "form-control" %>
              </div>
              <div class="form-group">
                <%= f.label :画像 %>
                <%= f.file_field :avatar, class: "form-control", accept: 'image/*', onchange: 'previewFileWithId(preview)' %>
                <%= f.hidden_field :avatar_cache %>
                <div class='mt-3 mb-3'>
                  <%= image_tag @user.avatar.url , size: '100x100', class: 'rounded-circle' %>
                </div>
              </div>
              <%= f.submit "更新", defaults:"update", class: "btn btn-primary"%>
            <% end %>  
          </div>
        </div>
      </div>
    </main>